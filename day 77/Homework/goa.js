function printPositions(row, col) {
    for (let i = 1; i <= row; i++) {
        for (let j = 1; j <= col; j++) {
            console.log(`row: ${i} col: ${j}`);
        }
    }
}

// მაგალითი:
printPositions(2, 2);
